from setuptools import setup

setup(name='ConsensusEnv',
      version='0.0.2',
      install_requires=['gym']  # And any other dependencies foo needs
)

# setup(name='ConsensusContEnv',
#       version='0.0.1',
#       install_requires=['gym']  # And any other dependencies foo needs
# )